## 问题分析
1. **API 400错误**：IndigoWorkshop.tsx中调用save-recent API时使用了`clothId`参数名，但后端API期望的是`cloth_id`（下划线分隔）
2. **提示显示问题**：虽然代码中有toast提示，但由于API调用失败，可能导致提示不显示
3. **保存到背包功能**：需要确保"保存到背包"操作有明确的成功/失败提示

## 修复计划

### 1. 修复API调用参数名
- **文件**：`IndigoWorkshop.tsx`
- **位置**：第83-86行
- **修改**：将API请求体中的`clothId`改为`cloth_id`，与后端API参数名匹配

### 2. 优化toast提示显示
- **检查**：确保所有API调用的成功/失败分支都有toast提示
- **优化**：添加更详细的错误信息，便于调试
- **统一**：确保所有提示都在右下角，样式一致

### 3. 确保"保存到背包"功能有明确提示
- **检查**：`ScoreResultDialog.tsx`中的`handleSaveToInventory`函数
- **验证**：确保保存到背包的API调用有正确的toast提示

### 4. 添加错误处理
- **增强**：在API调用中添加更全面的错误捕获
- **日志**：添加适当的错误日志，便于调试

### 5. 测试验证
- **运行开发服务器**：确保修复后API调用成功
- **验证提示**：确认成功和失败情况下都有正确的toast提示
- **检查功能**：验证保存到背包功能正常工作

## 预期效果
1. API调用不再返回400错误
2. 所有API操作都有明确的toast提示
3. "保存到背包"功能有清晰的成功/失败反馈
4. 提示统一显示在右下角，不影响游戏体验