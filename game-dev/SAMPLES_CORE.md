# 🎨 示例作品系统

**状态**: ✅ 已实现真实预览

---

## 📋 核心功能

**5个精选作品** → 真实预览 → 一键使用 → 快速创作

### 作品列表
1. ❄️ **冬日雪境**（初级）7图案 - 对称构图，清雅纯净
2. ✨ **星河璀璨**（中级）10图案 - 图案混搭，梦幻浪漫
3. 🌸 **花开盛夏**（高级）13图案 - 放射构图，华丽繁复
4. ⬡ **几何之美**（中级）11图案 - 蜂巢结构，现代简约
5. 🌊 **海浪涟漪**（初级）12图案 - 层次构图，自然流动

---

## 🚪 访问入口

### 主入口
工坊页面顶部右侧：**[✨ 查看示例作品]** 按钮

### 辅助入口
画布为空时，标题下方显示：**[✨ 查看示例作品]** 链接

### 直接访问
URL: `/game/samples`

---

## 🔄 使用流程

```
1. 点击按钮进入示例页面
2. 浏览作品（可按难度筛选）
3. 点击作品查看详情
4. 点击"使用此模板"
5. 自动跳转回工坊，图案已加载
6. 开始编辑和创作
```

---

## 🎨 真实预览实现

### 问题与解决
❌ **之前**: 空白背景 + emoji图标  
✅ **现在**: 真实渲染所有图案效果

### 核心代码
```typescript
// ArtworkPreview组件（与IndigoCanvas渲染一致）
{artwork.patterns.map((pattern) => {
  const PatternComponent = getPatternById(pattern.patternId).component
  const color = getIndigoColor(pattern.dyeDepth) // 5色阶映射
  
  return (
    <div style={{
      position: 'absolute',
      left: `${pattern.x}%`,           // 百分比定位
      top: `${pattern.y}%`,
      transform: 'translate(-50%, -50%)', // 中心对齐
      mixBlendMode: 'multiply'         // 蓝染混合
    }}>
      <PatternComponent 
        color={color}
        opacity={pattern.opacity}
        scale={pattern.scale}          // 组件自己处理
        rotation={pattern.rotation}
      />
    </div>
  )
})}
```

### 颜色映射
```
depth 0-20%  → #C5D5E4 浅蓝
depth 20-40% → #8FA9C3 中浅
depth 40-60% → #5B7FA1 中等
depth 60-80% → #3D5A80 深蓝
depth 80-100%→ #1E4D8B 极深
```

---

## 📁 核心文件

```
lib/game/
  sample-artworks.ts          作品数据定义

components/game/samples/
  ArtworkPreview.tsx          预览组件
  SampleGallery.tsx           展示画廊

app/game/samples/
  page.tsx                    示例页面
  
components/game/workshop/
  IndigoWorkshop.tsx          工坊组件（加载示例数据）
  
app/game/workshop/
  page.tsx                    工坊页面（添加入口按钮）
```

---

## 💾 数据流转

```
示例页面：点击"使用此模板"
    ↓
localStorage.setItem('selectedSample', artwork)
    ↓
跳转到工坊
    ↓
useEffect 检测 localStorage
    ↓
转换为 PlacedPattern[]
    ↓
setPatterns(loadedPatterns)
    ↓
画布自动渲染
    ↓
清除 localStorage
```

---

---

## 🐛 已解决的问题

### 问题1：无法看到作品效果
- **现象**：只显示空白背景 + emoji
- **原因**：没有实现预览渲染
- **解决**：创建 ArtworkPreview 组件

### 问题2：不知道如何访问
- **现象**：找不到示例作品入口
- **原因**：缺少导航按钮
- **解决**：添加顶部按钮 + 智能提示

### 问题3：无法使用模板
- **现象**：点击后没有反应
- **原因**：数据流转未实现
- **解决**：localStorage + useEffect

---

---

## ✅ 核心要点

1. **真实预览** - 100%还原工坊效果，激发灵感
2. **多入口设计** - 顶部按钮 + 智能提示，易于发现
3. **一键使用** - localStorage传递数据，无缝集成
4. **准确渲染** - 与IndigoCanvas完全一致的定位和混合

**让用户看到真实作品，一键开始创作！** 🎨
